/*
package com.java.DS1;

public class MyLinkedList {
    */
/**
     * 链表中的一个节点
     *//*

    public class Node{
        public int value;   //有效数据
        public Node next;   //下一个节点的线索(地址)
        Node (int v){
            this.value = v;
            this.next = null;
        }
    }

    //如果一个节点都没有   head =null
    private Node head;  //保存链表中第一个节点的引用

    MyLinkedList(){
        this.head =null;

    }

    void pushFront(){

    }

    void popFront(){
        if(head == null){
            throw new Error();
        }
        this.head = this.head.next;
    }
    //找最后一个节点   next == null
    void pushBack(int item){
        Node node;
        Node cur = this.head;
        while(cur.next != null){
            cur = cur.next;
        }
        cur.next = node;
    }

}
*/
